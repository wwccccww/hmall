server:
  port: 8080


hm:
  jwt:
    location: classpath:hmall.jks
    alias: hmall
    password: hmall123
    tokenTTL: 30m
  auth:
    excludePaths:
      - /search/**
      - /users/login
      - /items/**
      - /hi
      - /coupons   # GET 优惠券列表（领券中心公开浏览，无需登录）

spring:
  output:
    ansi:
      enabled: ALWAYS