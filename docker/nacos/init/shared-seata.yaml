# Seata 客户端配置（与 TC 通过 Nacos 发现对齐）。
# 1）TC（seata-server）须已注册到 Nacos，application 名与下方 registry.nacos.application 一致。
# 2）tx-service-group 与 vgroup-mapping 的 key 须一致（此处为 hmall）。
# 3）data-source-proxy-mode: XA 为强一致 XA 分支；若驱动/连接池报 XA 相关错误，可改为 AT（并保留 hm-item/hm-trade 的 undo_log 表）。
seata:
  enabled: true
  application-id: ${spring.application.name}
  tx-service-group: hmall
  enable-auto-data-source-proxy: true
  data-source-proxy-mode: XA
  # 客户端从哪读 Seata 服务端侧「全局配置」：未单独接 Nacos-Seata 配置中心时用 file 即可
  config:
    type: file
  registry:
    type: nacos
    nacos:
      # 与微服务一致：Compose 内置 Nacos 用 nacos:8848；虚拟机单机可设 SPRING_CLOUD_NACOS_SERVER_ADDR 或沿用默认
      server-addr: ${SPRING_CLOUD_NACOS_SERVER_ADDR:192.168.116.130:8848}
      namespace: ""
      group: DEFAULT_GROUP
      application: seata-server
      # Nacos 未开启鉴权（如 compose 中 NACOS_AUTH_ENABLE=false）时不要填账号，否则会请求 /v1/auth/users/login 报错。
      # 开启鉴权后通过环境变量注入：NACOS_USERNAME、NACOS_PASSWORD（或在各服务里配置同名占位符）
      username: ${NACOS_USERNAME:}
      password: ${NACOS_PASSWORD:}
  service:
    vgroup-mapping:
      hmall: default
